apply from: "$project.rootDir/shared/build.gradle"

android {
    defaultConfig {
        applicationId "yang.idvincent.ndk_template.app_native_activity"
        externalNativeBuild {
            cmake {
                arguments "-DBUILD_NATIVE_ACTIVITY=TRUE"
            }
        }
    }
}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
}
